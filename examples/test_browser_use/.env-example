# Test Browser Use Example - Environment Configuration
# Copy this file to .env and configure your values

# =============================================================================
# LLM Configuration (Required)
# =============================================================================

# API key for OpenAI-compatible API (browser_use uses this)
OPENAI_API_KEY=<your-openai-api-key-here>

# Alternative: browser_use worker also accepts API_KEY
# API_KEY=<your-api-key-here>

# =============================================================================
# Search Provider (Optional - only needed if use_search_tool=True)
# =============================================================================

# Tavily API key for multi-query link discovery
# Requires: `poetry add tavily-python`
TAVILY-PYTHON-RESEARCH-API-KEY=<your-tavily-api-key-here>
# Alternative env var name:
# TAVILY_API_KEY=<your-tavily-api-key-here>

# SerpApi fallback (optional)
# Requires: `poetry add google-search-results`
SERPAPI_API_KEY=<your-serpapi-api-key-here>

# =============================================================================
# Langfuse Observability (Optional)
# =============================================================================
# Set to "false" to disable Langfuse tracing entirely
LANGFUSE_ENABLED=false

# Langfuse credentials (if enabled)
LANGFUSE_PUBLIC_KEY=<your-langfuse-public-key>
LANGFUSE_SECRET_KEY=<your-langfuse-secret-key>
LANGFUSE_HOST=http://localhost:3000

